<!DOCTYPE html>

<title>New Tickets</title>

{% block body %}
    <h1>Create new Tickets</h1>

    {{ include('tickets_controller2/_form.html.twig') }}

    <a href="{{ path('app_tickets_controller2_index') }}">back to list</a>
{% endblock %}
