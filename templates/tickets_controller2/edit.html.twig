<!DOCTYPE html>

<title>Edit Tickets</title>

{% block body %}
    <h1>Edit Tickets</h1>

    {{ include('tickets_controller2/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_tickets_controller2_index') }}">back to list</a>

    {{ include('tickets_controller2/_delete_form.html.twig') }}
{% endblock %}
