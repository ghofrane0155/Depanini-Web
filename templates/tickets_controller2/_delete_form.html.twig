<form method="post" action="{{ path('app_tickets_controller2_delete', {'idticket': ticket.idticket}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ticket.idticket) }}">
    <button class="btn">Delete</button>
</form>
