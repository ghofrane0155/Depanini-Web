<!DOCTYPE html>

<title>Contrat</title>

{% block body %}
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdContart</th>
                <td>{{ contrat.idContart }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ contrat.date ? contrat.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ contrat.Etat }}</td>
            </tr>
            <tr>
                <th>Conditions</th>
                <td>{{ contrat.conditions }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contrat_index') }}">back to list</a>

    <a href="{{ path('app_contrat_edit', {'idContart': contrat.idContart}) }}">edit</a>

    {{ include('contrat/_delete_form.html.twig') }}

    
{% endblock %}
