<form method="post" action="{{ path('app_contrat_delete', {'idContart': contrat.idContart}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat.idContart) }}">
    <button class="btn">Delete</button>
</form>
