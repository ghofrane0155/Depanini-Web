<!DOCTYPE html>

<title>Recrutements</title>

{% block body %}
    <h1>Recrutements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Reference</th>
                <td>{{ recrutement.reference }}</td>
            </tr>
            <tr>
                <th>Salaire</th>
                <td>{{ recrutement.salaire }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recrutement.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ recrutement.date ? recrutement.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ recrutement.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recrutements_index') }}">back to list</a>

    <a href="{{ path('app_recrutements_edit', {'reference': recrutement.reference}) }}">edit</a>

    {{ include('recrutements/_delete_form.html.twig') }}
{% endblock %}
